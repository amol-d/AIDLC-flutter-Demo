name: aidlc_demo_workspace
description: Workspace root for the AIDLC-flutter-Demo monorepo. Not a publishable package.
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"

dev_dependencies:
  melos: ^6.3.2
