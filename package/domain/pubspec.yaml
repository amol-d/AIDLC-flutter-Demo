name: domain
description: Domain layer - entities, use cases, repository contracts, and navigation abstractions.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.4
  get_it: ^7.7.0
  injectable: ^2.4.2
  meta: ^1.18.0
  shared:
    path: ../shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.11
  flutter_lints: ^5.0.0
  freezed: ^2.5.7
  injectable_generator: ^2.6.2
  mocktail: ^1.0.4
